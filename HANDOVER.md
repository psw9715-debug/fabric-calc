# 작업 지시서 — 모바일 전용 원격 작업

> **이 문서는 CLAUDE.md를 보완합니다.**
> 프로젝트 구조·데이터 모델·계산 엔진 설명은 `CLAUDE.md`를 먼저 읽어주세요.
> 이 문서는 **작업 환경 변경 사항**과 **이번 작업 지시**만 담고 있습니다.

---

## 1. 작업 환경이 바뀌었습니다 (중요)

### 로컬 PC 없음
- 기존 로컬 작업 폴더 `D:\프로그램2\꾀꼬리\fabric-calc` — **더 이상 존재하지 않습니다.**
- CLAUDE.md에 언급된 중첩 클론 `fabric-calc/fabric-calc/` — 로컬 이야기이므로 **무시하세요.**
- 앞으로 모든 작업은 **GitHub 저장소를 직접 대상으로** 합니다.

### 유일한 정본
```
https://github.com/psw9715-debug/fabric-calc
브랜치: main
편집 대상: index.html (최상위 파일 하나만)
```

### 작업 방식
사용자는 iPhone에서 Claude 모바일 앱으로 작업합니다.
- 부분 수정 지시(예: "123번째 줄을 이렇게 바꿔")는 **모바일에서 적용 불가능**합니다.
- 코드를 전달할 때는 **항상 완성된 `index.html` 전체 파일**로 주세요.
- 사용자는 GitHub 웹 편집기(연필 아이콘)에서 전체를 붙여넣고 커밋합니다.
- 커밋 후 1~2분 내 GitHub Pages에 자동 반영됩니다.

---

## 2. 저장소 정리 (선행 작업)

### 삭제할 파일
```
fabric_v4_final.html   ← 구버전. index.html과 혼동을 유발하므로 삭제
```

`index.html`이 유일한 앱 파일입니다.
`products.json`은 Gist 동기화 산출물이므로 손대지 마세요.

### 정리 후 저장소 구조
```
fabric-calc/
├── index.html      ← 앱 전체 (유일한 편집 대상)
├── CLAUDE.md       ← 프로젝트 가이드
├── HANDOVER.md     ← 이 문서
├── README.md
└── products.json   ← Gist 산출물 (편집 금지)
```

---

## 3. 이번 작업 — 원단 관리 구조 변경

### 배경

현재는 제품 등록 화면에서 피스마다 원단을 입력합니다.
그런데 실무에서는 **원단이 업체 단위로 고정**됩니다.

예: 혀니네홈패션은 소창(110cm), 면혼방(150cm)을 주로 씁니다.
제품마다 매번 같은 원단 정보를 입력하는 건 낭비입니다.

### 변경 방향

```
[변경 전]
제품 등록 → 피스마다 원단 이름/폭 입력
계산 화면 → 원단 폭 직접 입력

[변경 후]
업체 세팅 → 업체별 원단 목록 관리 (한 번만 등록)
제품 등록 → 원단 입력란 제거 (피스 치수만 입력)
계산 화면 → 등록된 원단 버튼으로 선택 or 수동 입력
```

### 3-1. 제품 등록 화면 변경

`screen-register`의 피스 카드에서 **원단 관련 입력란을 제거**합니다.

피스에 남는 항목:
- 피스 이름
- 가로 / 세로 (cm)
- 1세트당 수량
- 식서방향 토글 (회전가능 / 식서고정)

### 3-2. 업체 세팅 화면 강화

`screen-manage`에서 업체별 원단 목록을 관리합니다.
(이미 `fabric_vendors_v1` 구조가 있으므로 이를 주 저장소로 사용)

```
혀니네홈패션
  ├─ 소창        110cm   [수정] [삭제]
  ├─ 면혼방      150cm   [수정] [삭제]
  └─ + 원단 추가
```

- 원단 이름은 **자유 입력**입니다.
  "겉지/속지"뿐 아니라 "호피무늬", "줄무늬", "소창" 등 무엇이든 가능합니다.
- 폭은 cm 단위 숫자.

### 3-3. 계산 화면 원단 선택 UI

`screen-calc`의 원단 폭 입력 부분:

```
원단 폭 (cm)
[소창 110] [면혼방 150]      ← 해당 업체에 등록된 원단
┌──────────────┐
│     150      │             ← 탭하면 자동 입력, 직접 수정도 가능
└──────────────┘
```

**동작 규칙**
- 버튼 탭 → 입력란에 폭 자동 입력 + 해당 버튼 활성 표시
- 숫자를 직접 수정하면 → 버튼 선택 해제 (수동 입력 모드)
- 해당 업체에 등록된 원단이 없으면 → 버튼 없이 입력란만 표시
- 이름+폭이 완전히 같은 원단은 중복 표시하지 않음

### 3-4. 데이터 마이그레이션

`migrateProducts()`를 확장해서 기존 데이터를 처리합니다.

```
기존 제품의 piece.fabrics[] 에 원단 정보가 있으면
  → 해당 제품의 company 의 fabric_vendors_v1 목록으로 이관
  → 이관 후 piece.fabrics 는 제거 (또는 무시)
  → 동일 이름+폭은 중복 등록하지 않음
company 가 빈 문자열인 제품의 원단은 '미분류' 업체로 이관
```

기존 사용자 데이터가 유실되지 않는 것이 최우선입니다.

---

## 4. 작업 시 반드시 지킬 것

### 기술 제약
| 항목 | 규칙 |
|------|------|
| 파일 구조 | `index.html` 단일 파일 (HTML+CSS+JS) |
| 라이브러리 | 외부 라이브러리·CDN 일체 금지 (작업장 오프라인 대응) |
| JS 문법 | `var` 사용 (`let`/`const` 아님 — 기존 스타일 통일) |
| 이벤트 | 인라인 `onclick="fn()"` 방식 |
| HTML 생성 | 문자열 연결 (`html += '...'`) |
| XSS 방어 | 사용자 입력은 반드시 `esc()` 통과 |
| 토큰 | GitHub 토큰 하드코딩 절대 금지 (공개 저장소 → 자동 차단됨) |

### 모바일 UI (iPhone 13 mini / 375px / Safari)
| 항목 | 값 |
|------|-----|
| 일반 버튼 | `min-height: 44px` |
| 주요 버튼 | `min-height: 56px` |
| 입력창 | `min-height: 52px`, `font-size: 18px` (iOS 자동확대 방지) |
| 헤더 상단 | `padding-top: 52px` |
| 하단 고정 | `padding-bottom: max(14px, env(safe-area-inset-bottom))` |
| 터치 | `touch-action: manipulation` |

색상은 `:root`의 CSS 변수(`--bg`, `--surface`, `--accent`, `--danger`, `--warn` 등)를 재사용하세요. 새 색상 추가 금지.

---

## 5. 완료 체크리스트

작업을 마치기 전에 확인하세요.

- [ ] `fabric_v4_final.html` 삭제됨
- [ ] 제품 등록 화면에서 원단 입력란 제거됨
- [ ] 업체 세팅에서 원단 추가/수정/삭제 가능
- [ ] 계산 화면에 원단 선택 버튼 표시됨
- [ ] 버튼 탭 → 폭 자동 입력 동작
- [ ] 직접 입력 시 버튼 선택 해제 동작
- [ ] 등록된 원단 없을 때도 정상 동작 (입력란만 표시)
- [ ] 기존 제품 데이터 마이그레이션 정상 동작
- [ ] 375px 화면에서 레이아웃 깨짐 없음
- [ ] 외부 라이브러리 사용 없음
- [ ] 토큰 하드코딩 없음
- [ ] 결과물이 **완성된 index.html 전체 파일**로 전달됨

---

## 6. 전달 형식

작업 완료 시 다음 형식으로 전달해주세요.

1. **변경 요약** — 무엇을 어떻게 바꿨는지 3~5줄
2. **주의사항** — 사용자가 알아야 할 동작 변화가 있으면 명시
3. **완성된 `index.html` 전체 코드** — 복사해서 GitHub에 그대로 붙여넣을 수 있는 상태

부분 수정 패치나 diff 형태로는 전달하지 마세요.
사용자가 모바일에서 적용할 수 없습니다.
